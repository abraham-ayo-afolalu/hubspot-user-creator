import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Elastic Path Customer Portal User Creator",
  description: "Create users in HubSpot for Elastic Path Customer Portal with organization matching",
  icons: {
    icon: "/Elastic Path FAVICON_64x64.svg",
    shortcut: "/Elastic Path FAVICON_64x64.svg",
    apple: "/Elastic Path FAVICON_64x64.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
